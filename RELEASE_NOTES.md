# 星图 StarChart 版本说明

> 本地工作区项目导航工具：扫描工作区、识别项目、AI 生成中文介绍、一键跳转编辑器 / Agent / 终端。

---

## v1.3.0（2026-09-02）

### 新增

- **启动状态持久化**：运行中的项目 PID 随 `data.json` 顶层 `running` 字段持久化，
  服务重启时自动校验进程存活并恢复——之前启动的项目不会变成孤儿进程，
  「● 运行中 / ■ 停止」在重启后依然有效；进程自然退出会顺带清理记录。
- **多扫描根分组**：配置多个扫描根时，树上按根显示分组头（粗体根名 + 根路径），
  点击可整根收起 / 展开，折叠状态本地记忆；搜索时忽略折叠，直接平铺命中结果。
- **API Key 加密存储**：API Key 改用 Windows DPAPI 加密后写入 `config.json`
  （仅本机当前用户可解密），导出的备份包内含的也是密文，不再出现明文 Key。

### 兼容性说明

- 服务端有改动，升级后需重启星图服务（运行 `stop.bat` 后再运行 `start.bat`）。
- `data.json` 顶层新增可选 `running` 字段，旧数据无需改动。
- `config.json` 中已有的明文 API Key 会在下次保存时自动升级为 DPAPI 加密。
- 注意：备份包**跨机器 / 跨用户恢复后 API Key 无法解密**，需在设置中重新填写。

---

## v1.2.0（2026-09-02）

### 变更：数据目录改为随工具走

数据目录由「`server.py` 往上两级的 `.starchart\`」改为**工具文件夹内的 `starchart\.starchart\`**，
工具自包含——拷走整个 starchart 文件夹即为完整迁移，挪动位置不再丢失索引。
可用环境变量 `STARCHART_HOME` 改到任意位置（此时不自动迁移）。

首次启动新版会**自动迁移**：把旧目录（如 `d:\work\.starchart\`）的 `data.json` 与
`config.json` 复制进工具目录，只复制不删除，旧目录原样保留，确认无误后可自行清理。
已存在同名文件时不会被覆盖。

> 代价：数据与代码现在同处一地，**升级工具时不要删掉 `.starchart\` 目录**。

### 新增

- **AI 批量生成并发化**：批量生成改由后端 3 路并发执行，前端轮询进度，
  100 个项目的耗时约为原来的三分之一；停止立即生效。
- **常用视图**：顶栏「🌳 树 / ★ 常用」一键切换。每次通过星图打开或启动项目都会记一次使用，
  按使用频率排序（越久没用权重越低），收藏的项目永远置顶。
- **收藏 / 标签 / 备注**：详情面板可给项目加 ★ 收藏、最多 10 个标签、500 字备注，
  全部可被搜索；重新扫描不丢失。
- **项目内查找文件**：详情面板新增「查找文件」，按文件名即时过滤
  （跳过 node_modules 等重量级目录），点击定位所在目录或复制完整路径。
- **技术栈徽章**：扫描时根据标志文件与依赖推断主语言 / 框架
  （React / Vue / Python / Go / Rust / Java / PHP / .NET / C/C++ / Node.js），
  树上直接可见，详情面板显示完整列表。
- **服务日志**：`server.log` 记录跳转 / 启动 / 停止 / 备份 / 被拒绝的跨站请求，
  超过 1MB 自动轮转一份旧日志。
- **搜索命中高亮**：命中的项目名称与介绍片段以高亮底色标出。
- **Git 状态**：扫描时读取每个项目的分支、未提交更改、领先 / 落后远程、最后提交时间，
  树上给有未提交更改的项目标一个橙点，详情面板显示完整状态。
  并发读取，**git 不存在或单个命令超时（2 秒）时静默跳过**，不影响其余功能；
  设置里可关闭（默认开启）。
  明确边界：Git 状态是**展示属性而非判定依据**——没有 `.git` 的项目照样是项目，
  只是不显示 Git 信息。
- **托盘常驻 + 全局热键**：新增 `tray_app.py` 常驻入口。启动后无控制台窗口，
  托盘图标提供「打开星图 / 重新扫描 / 打开数据目录 / 退出」，
  全局热键 **Ctrl+Alt+S** 可随时唤起界面。
  热键走 ctypes 直调 win32 `RegisterHotKey`，因此**新增依赖只有 `pystray` 一个**
  （无需 `keyboard` 之类要管理员权限的库）；未安装时自动回落到无托盘模式，
  服务与热键照常可用。
- **`start-console.bat`**：保留控制台窗口的启动方式，便于查看日志与排查启动问题。
- **扫描结果可见性**：扫描根失败时保留该根上一次的结果并提示数量（原为整根消失）；
  目录层级过深被截断时提示截断处数。
- **索引过期提醒**：索引生成超过 24 小时后，界面显示一条提示并给出「重新扫描」按钮
  （可「本次忽略」）。**仍不自动扫描**——是否扫描由用户决定。

### 修复

- **搜索结果被折叠的祖先挡住**：命中项所在分支现在自动展开。该展开仅渲染期生效，
  不写入本地存储，清空搜索后恢复原状。
- **弹窗确认框重复弹出**：设置 / 备份弹窗开关多次后，按一次 Esc 会连续弹多个确认框。
- **配置文件损坏被静默丢弃**：改为先隔离成 `.corrupt-*` 再回落默认配置；
  配置写入同时改为原子写（与 `data.json` 一致）。
- **待生成介绍数量前后端不一致**：补齐「内容已变更」这一判定分支。
- **排除 / 恢复后无提示**：现在明确提示需重新扫描才完全生效。
- **`stop.bat` 改端口后失效**：改为按进程命令行精确匹配，既不依赖端口，
  也不会因端口被别的程序占用而误杀。
- **启动脚本乱码 / 解析错乱（根因修复）**：cmd 用「活动代码页」解码批处理文件，
  而活动代码页因环境而异（双击是 936、配置过 UTF-8 的 PowerShell 里是 65001）——
  含中文的 .bat 在错误代码页下行边界错位，提示文字会被当成命令执行。
  因此三个 .bat 改为**纯 ASCII 英文提示**（任何代码页都安全），
  中文反馈改由 Python 输出（不受代码页影响，运行 start-console.bat 可见）；
  同时把 stop 的进程匹配逻辑拆到独立的 `stop.ps1`——此前内嵌在 .bat 里的
  PowerShell 管道符会被 cmd 的引号转义问题拆碎，导致**解析失败却仍显示"已停止"**。

### 安全加固

- **编辑器 / Agent 可执行路径白名单**：`/api/open` 只启动本机探测到的程序，
  拒绝请求体中传入的任意可执行路径。
- **扫描并发保护**：同一时刻只允许一次扫描，避免与介绍生成产生竞态。

### 兼容性说明

- 服务端有改动，升级后需重启星图服务（运行 `stop.bat` 后再运行 `start.bat`）。
- 首次启动会自动迁移旧数据目录，旧目录保留不删。
- `config.json` 新增可选键 `gitStatus`（默认开启）；`data.json` 顶层新增 `usage`
  （使用记录）与节点上的 `starred` / `tags` / `note` / `stack` 字段，旧数据无需改动。
- 界面打磨：悬停 / 键盘焦点改用更淡的强调色（与主行动按钮区分）、亮色主题层级拉开、
  树行加高到约 28px、路径与代码改用等宽字体、窄屏（<900px）自动切单栏、
  空状态与搜索无结果都给了可点的下一步动作、树支持 Home / End 键导航、
  尊重系统「减少动态效果」设置。

---

## v1.1.0（2026-09-01）

### 新增

- **界面主题「跟随系统」**：设置中的主题选项扩展为 暗色 / 亮色 / 跟随系统 三档。
  选择「跟随系统」后，界面随操作系统 `prefers-color-scheme` 实时切换，无需刷新页面。

### 修复

- **重复扫描报 "not found"**：修复扫描请求被误发为 GET 导致 404 的问题，扫描功能恢复正常。
- **Trae 启动报错**：修复带空格安装路径的编辑器（如 `Trae CN`、`TRAE SOLO CN`）启动失败的问题；
  `.cmd` 启动脚本改经 PowerShell 调用，规避 cmd.exe 引号剥离缺陷。
- **Agent 检测不全**：新增注册表桌面版 Agent 应用扫描，现在可识别 WorkBuddy、ZCode、DSH、
  MiniMax Code、Codex++、CodeBuddy 等未提供 CLI 命令的桌面端 Agent，并支持直接启动；
  项目证据匹配同步覆盖（如 CodeBuddy 的 `buddycn` 命令）。
- **僵尸「运行中」状态**：项目进程自然退出后，详情页不再永久显示「运行中」；
  现在会自动检测进程存活并清理过期的运行记录。
- **「停止」按钮状态**：生成介绍过程中的停止按钮显隐逻辑修正，扫描时不再误显示。

### 安全加固

- **跨站请求防护（CSRF）**：所有 POST 接口校验 `Origin` 头，拒绝来自非本机页面的
  跨站请求，防止恶意网页利用本机服务执行命令。
- **启动入口参数白名单**：`/api/launch` 仅接受扫描时生成的安全格式参数
  （字母 / 数字 / 点 / 横线），杜绝通过请求体注入任意命令。
- **命令注入防护**：PowerShell 单引号字符串统一转义（`'` → `''`），
  编辑器 / Agent 可执行文件路径与项目路径中的引号、换行等危险字符一律拒绝。

### 兼容性说明

- `config.json` 新增合法主题值 `"auto"`；旧配置文件无需修改，缺省仍为暗色。
- 服务端有改动，升级后需重启星图服务（运行 `stop.bat` 后再运行 `start.bat`）。

---

## v1.0.0（2026-09-01）

首个正式版本。

### 核心功能

- **项目扫描与识别**：基于项目标记文件（.git、package.json、requirements.txt 等）自动识别项目，
  支持手动标记 / 排除，重扫后标记保留。
- **中文项目介绍**：支持 AI 生成（智谱 / 硅基流动 / 自定义 OpenAI 兼容接口）、README 静态提取、
  手动编辑三种来源；批量生成带进度条与停止按钮。
- **树形导航**：与目录结构一致的左侧树视图，含项目图标 🛠、文件数统计、拼音搜索
  （名称 / 介绍 / 路径 / 拼音首字母，如 `zhjy`）。
- **快速跳转**：资源管理器定位、复制路径、打开终端、用已检测到的编辑器
  （Trae / VS Code / Cursor / Windsurf / Qoder 等）打开项目。
- **Agent 检测**：自动检测本机 12+ 终端 Agent CLI（Claude Code、Codex、Gemini CLI 等），
  按项目内配置文件证据（CLAUDE.md、.cursor 等）智能匹配显示；含通用配置（AGENTS.md）回退逻辑。
- **项目启动**：自动检测 npm / python / bat / docker compose / make 启动入口，
  新终端窗口启动并显示运行状态（PID、停止按钮）。
- **备份与恢复**：一键导出 `starchart-YYYYMMDD.zip`（含 data.json 与 config.json），
  从备份包恢复（自动生成 .bak），支持常用备份目录。
- **暗色 / 亮色主题**：手动切换，设置即存即生效。

### 技术要点

- 纯 Python 标准库后端（可选依赖 `pypinyin` 提升拼音搜索），单文件服务，仅监听 127.0.0.1。
- 单页面前端，无构建步骤；静态资源 `Cache-Control: no-store` + 版本号防缓存。
- 数据存储于 `~/.starchart/`（或工作区根 `.starchart/`）下的 JSON 文件，原子写入防损坏。
